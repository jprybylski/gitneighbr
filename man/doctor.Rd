% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/doctor.R
\name{doctor}
\alias{doctor}
\title{Diagnose whether gitneighbr can run against a repository}
\usage{
doctor(path = ".", git = getOption("gitneighbr.git", unname(Sys.which("git"))))
}
\arguments{
\item{path}{Path inside the Git working tree to serve. Defaults to the
current directory.}

\item{git}{Path to the \code{git} executable.}
}
\value{
An invisible \code{gitneighbr_doctor_report}: a list with \code{ok} (\code{TRUE}
unless any non-advisory check failed) and \code{checks}, a list of
\code{gitneighbr_doctor_check} objects (\code{id}, \code{status} — one of \code{"ok"},
\code{"fail"}, \code{"advisory"}, \code{"skipped"} — \code{message}, and an optional
\code{detail} list). Printed automatically; print again with \code{print()}.
}
\description{
Runs the same read-only checks \code{\link[=open_repo]{open_repo()}} relies on, without starting a
server or mutating the repository: Git presence and version, repository
detection (including distinguishing a bare repository from no repository
at all), upstream configuration, whether the remote looks like GitHub,
credential-helper presence, and active commit hooks.
}
